 export class Product {
 	constructor(identifier, family, categories, values) {
 		this.identifier = identifier;
 		this.family = family;
 		this.categories = categories;
 		this.values = values;
 	}
 }